Both group members worked on each algorithm simulantiously. If one person
was typing then the other was making suggestions as well.

The change that was made was a new function and it's helper function. It 
combinded the minamax function and the heuristic one. The function ran minamax
and then combined the heuristic values for both moves,